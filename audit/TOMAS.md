# Tomas.zip

Vehicle: `Tom on Car` (space in folder name — leave)
Not a walker — kept **active**.

Stripped targets: `info_2D.json`, `info_RC_car.json`  
Game had zip locked — original bak + stripped swap ready:

- bak: `mods\_parked\Tomas.zip_pre_info_strip_*`
- swap: `mods\_parked\Tomas.zip_STRIPPED_ready_swap`

After BeamNG close: replace `mods\Tomas.zip` with the stripped swap.
